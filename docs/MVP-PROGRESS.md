# 排球隊管理 — MVP 進度追蹤

> 對照規格：`docs/volleyball-team-manager/01-mvp-pages.md`（若規格在 monorepo 其他路徑請自行調整連結）。  
> 使用方式：完成項目改為 `[x]`，進行中可在文字後標註「進行中」。

**最後更新**：2026-05-09（A7 隊伍設定／通知偏好已接上 `/coach/team`）

---

## 圖例

- `[x]` 已完成（或可視為 M1 已滿足最小閉環）
- `[ ]` 未開始或未完成
- `[~]` 部分完成（若有使用）

---

## 共同基礎

| 狀態 | 項目 |
|------|------|
| [x] | Clerk 登入、`getTeamMember`、Prisma `User.clerkUserId` 與 Email 合併 |
| [x] | 開發除錯：`ALLOW_DEBUG_AUTH`、`x-debug-*`／cookie |
| [x] | 開發：`ALLOW_BOOTSTRAP`、`POST /api/bootstrap` |
| [x] | 開發：`POST /api/bootstrap/claim`（依 Clerk 帳號加入示範隊） |
| [x] | 共用 `/onboarding`（無 `TeamMember` 時） |
| [x] | Middleware：`/coach`、`/player`、`/onboarding` 保護 |
| [x] | 多隊切換 UI（`active-team-id` cookie + `/api/me/active-team` + 教練／球員 header 與首頁） |
| [x] | 正式環境關閉 bootstrap（`isBootstrapAccessEnabled` + Vercel/APP_ENV）+ 教練端「隊伍」依 Email 建隊籍 |

---

## 教練端（Admin / Coach）

### A1 Dashboard

| 狀態 | 項目 |
|------|------|
| [x] | 未來 7 天事件列表 |
| [x] | 草稿事件數量 |
| [x] | 各場 RSVP 未回覆統計、「待追蹤」區塊與連結 |
| [x] | 今日訓練 RPE／疲勞／疼痛儀表（簡易） |
| [x] | 可配置儀表板（本機顯示區塊）、近 30 天 RPE 趨勢圖（SVG） |

### A2 行事曆

| 狀態 | 項目 |
|------|------|
| [x] | 週視圖（週一起算）、列表視圖、月視圖 |
| [x] | 篩選：事件類型、分組（參與者 `squad`） |
| [x] | 點擊事件 → 教練事件詳情 |
| [~] | 拖曳改期、外部日曆同步（M2+）；**複製事件／複製上週訓練（+7 天）已實作** |

### A3 事件列表

| 狀態 | 項目 |
|------|------|
| [x] | 列表、基本欄位、新增事件入口 |
| [x] | 搜尋（標題／地點）、日期區間與進階篩選 |
| [ ] | 批次編輯、匯出 CSV（M2+） |

### A4 建立／編輯事件

| 狀態 | 項目 |
|------|------|
| [x] | 建立草稿、類型、時間、場館、參與者（建立時 ALL） |
| [x] | RSVP 截止（建立時可填） |
| [x] | `PATCH` 更新事件（含 RSVP 截止） |
| [x] | 發布事件 |
| [x] | 建立／編輯時完整參與者規則 UI（分組／指名，API 已有 participantRule） |
| [ ] | 附件／連結（M1 URL）（見 FileAsset 或欄位） |
| [ ] | 重複規則、地圖（M2+） |

### A5 事件詳情（教練）

| 狀態 | 項目 |
|------|------|
| [x] | 概述、時間地點、編輯、發布 |
| [x] | 出席／RSVP／點名表 |
| [x] | 訓練計畫編輯 + AI 產生 |
| [x] | 身體回饋彙總（RPE／疲勞／疼痛／明細表） |
| [x] | 公告／留言（`Comment` 模型） |
| [ ] | RSVP 快捷：全到、依 RSVP 一鍵轉實到 |
| [x] | 戰術板、影片整理（FileAsset 連結分類；教練增刪／球員唯讀） |
| [x] | 場上企位示意（`Event.courtSketch` JSON；半場 SVG、拖曳標記；教練編輯／球員唯讀） |

### A6 隊員管理

| 狀態 | 項目 |
|------|------|
| [x] | 隊員列表（`/coach/team`：姓名、Email、角色、背號、分組、Clerk 狀態） |
| [x] | 新增／編輯隊員（Email、顯示姓名、角色、背號、位置、分組、電話、備註、在籍／停用） |
| [ ] | 匯入／貼上名單、邀請信（Email link） |

### A7 隊伍設定

| 狀態 | 項目 |
|------|------|
| [x] | 隊名、賽季、分組（`groupConfig`）設定 UI |
| [x] | 角色管理介面、通知設定（Email） |

### 角色：隊務（STAFF）

| 狀態 | 項目 |
|------|------|
| [ ] | 教練端是否開放 STAFF、可讀寫範圍（目前以 `isCoachLike` 為主） |

---

## 球員端（Player）

### B1 我的行程

| 狀態 | 項目 |
|------|------|
| [x] | 列表（今日起 + 歷史）、已發布且參與者範圍 |
| [x] | RSVP 狀態提示（含截止後不顯示「待 RSVP」） |
| [x] | 週視圖／月視圖（M1 可選） |

### B2 事件詳情（球員）

| 狀態 | 項目 |
|------|------|
| [x] | 時間、地點、說明、RSVP 截止顯示 |
| [x] | RSVP 表單（含截止鎖定） |
| [x] | 訓練計畫唯讀 |
| [ ] | 集合時間顯著展示（若資料有 `meetAt` 可再強調） |
| [x] | 公告／留言 |
| [x] | 場上企位示意（唯讀；與教練儲存之 `courtSketch` 同步） |
| [ ] | 附件連結 |

### B3 回饋提交

| 狀態 | 項目 |
|------|------|
| [x] | 事件結束後可填、24 小時內可改（與 API 一致） |

### B4 我的回饋歷史

| 狀態 | 項目 |
|------|------|
| [x] | `/player/feedback` 列表、可編輯截止提示 |

### B5 個人資料

| 狀態 | 項目 |
|------|------|
| [ ] | 顯示／編輯聯絡方式等（多數欄位教練維護） |

---

## 跨功能能力（規格 C）

| 狀態 | 項目 |
|------|------|
| [x] | C1 事件：建立、編輯、發布、查詢（依角色） |
| [x] | C2 RSVP：球員 RSVP、教練點名、**截止時間** |
| [ ] | C2 提醒：RSVP 截止前 Email（需寄信 + 排程） |
| [x] | C3 訓練內容：結構化段落 + AI |
| [x] | C4 回饋：球員提交、教練單場彙總 |
| [ ] | C1 附件：M1 URL |

---

## 工程與上線

| 狀態 | 項目 |
|------|------|
| [ ] | API／E2E 測試補強 |
| [ ] | Clerk Production、網域與 After sign-in URL 檢查 |
| [ ] | 部署與環境變數文件（README 補齊與此表同步） |

---

## 快速統計（手動維護）

可自行填寫：

- **M1 核心路徑**（登入 → 事件 → 發布 → RSVP → 點名 → 回饋）：□ 可示範給隊上  
- **下一個建議實作**：□ A5 留言 □ A6 隊員 □ Email 提醒 □ 其他：___________
