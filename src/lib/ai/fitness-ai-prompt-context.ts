/** 體測情境（註解：體能「分析」prompt 用；不含訓練課表）。 */
export const FITNESS_TEST_MEASUREMENT_CONTEXT = `本隊體能測試在球隊訓練結束後進行。
因此體測數據多半反映「隊練後、疲勞狀態下」的表現；分析與排名時須納入此情境，勿與完全休息後的標準直接比較。`;

/** 隊伍體能訓練（註解：時段／時長由教練補充決定，不預設固定限制）。 */
export const TEAM_FITNESS_TRAINING_CONTEXT = `團體體能訓練的時段、每次時長、每週頻率、暖身／收操安排、場地與器材限制，一律以「教練補充」為準。
若教練未說明，勿自行假設固定時長（例如 30 分鐘）；可列出需教練確認的項目，或提供彈性方案並明確標註所採假設。`;

/** 隱私規則（註解：所有體能 AI prompt 共用）。 */
export const FITNESS_AI_PRIVACY_RULES = `- 輸入與輸出勿包含或臆測選手姓名、隊伍名稱、活動／事件名稱。
- 選手以 playerKey（如 P1、P2）與背號識別，評語中沿用相同代號。`;
