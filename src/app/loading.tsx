import { AppRouteLoading } from "@/components/app-route-loading";

/** 根層路由載入（註解：首頁、onboarding、sign-in 等頂層轉場）。 */
export default function RootLoading() {
  return <AppRouteLoading variant="neutral" />;
}
