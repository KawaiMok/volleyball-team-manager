import { AppRouteLoading } from "@/components/app-route-loading";

/** 教練端主視圖內容槽載入（註解：保留 header，僅替換主內容區）。 */
export default function CoachMainLoading() {
  return <AppRouteLoading variant="coach" />;
}
