import { AppRouteLoading } from "@/components/app-route-loading";

/** 球員端：事件詳情載入（註解：保留 header，僅替換主內容區）。 */
export default function PlayerEventDetailLoading() {
  return <AppRouteLoading variant="player" />;
}
